# Banco (Supabase Postgres) — connection string
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[REF].supabase.co:5432/postgres

# IA (Anthropic / Claude)
ANTHROPIC_API_KEY=

# ASAAS (REST) — usar sandbox primeiro
ASAAS_API_KEY=
ASAAS_BASE_URL=https://sandbox.asaas.com/api/v3

# E-mail (envio das e-mail-propostas) — definir provedor depois
# SMTP_URL=
